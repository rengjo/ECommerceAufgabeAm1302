/* utils.js */

// Hilfsfunktionen -methoden, die von anderen JavaScript-Dateien verwendet werden können

// Beispiel: Funktion zum Formatieren eines Preises in das Währungsformat
function formatPrice(price) {
    return '$' + price.toFixed(2);
}

// Beispiel: Funktion zum Hinzufügen eines Produkts zum Warenkorb
function addToCart(product) {
    // Code zum Hinzufügen des Produkts zum Warenkorb
}

// Beispiel: Funktion zum Berechnen des Gesamtpreises des Warenkorbs
function calculateTotalPrice(cart) {
    // Code zum Berechnen des Gesamtpreises basierend auf den Produkten im Warenkorb
}
